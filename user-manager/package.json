{
  "name": "user-manager",
  "version": "1.0.0",
  "description": "A backend microservice to managing users and games.",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "tsc --preserveWatchOutput --watch",
    "tsx-run": "tsx watch --tsconfig ./tsconfig.dev.json",
    "typecheck": "tsc --noEmit --project ./tsconfig.dev.json"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "dotenv": "^17.2.3",
    "drizzle-kit": "^0.31.5",
    "drizzle-orm": "^0.44.6",
    "fastify": "^5.6.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@tsconfig/node24": "^24.0.1",
    "@types/better-sqlite3": "^7.6.13",
    "tsx": "^4.20.6"
  }
}
